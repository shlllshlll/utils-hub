{
  "extends": "../tsconfig.json", // 继承根配置
  "compilerOptions": {
    "outDir": "./dist",     // 输出到 packages/core/dist
    "rootDir": "./src"      // 源码在 packages/core/src
  },
  "include": ["src/**/*"],  // 只处理 src 下的文件
  "exclude": ["node_modules", "**/*.test.ts"] // 排除测试文件
}